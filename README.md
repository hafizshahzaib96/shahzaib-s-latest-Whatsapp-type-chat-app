<!DOCTYPE html><html><head><title>Latest Chatbot</title><style>body {  margin: 0;  font-family: Arial;}.container {  display: flex;  height: 100vh;}.left {  width: 30%;  background: #075E54;  color: white;  padding: 15px;}.right {  width: 70%;  background: #ECE5DD;  display: flex;  flex-direction: column;}.chat-box {  flex: 1;  padding: 20px;  overflow-y: scroll;}.message {  margin: 10px 0;}.me {  text-align: right;}.bubble {  display: inline-block;  padding: 8px 12px;  border-radius: 10px;}.me .bubble {  background: #DCF8C6;}.other .bubble {  background: white;}input {  padding: 10px;  margin: 5px;}button {  padding: 10px;  background: #25D366;  border: none;  color: white;  cursor: pointer;}.user {  padding: 8px;  cursor: pointer;}.user:hover {  background: rgba(255,255,255,0.2);}</style></head><body><div id="login" style="text-align:center; margin-top:200px;">  <h2>Enter Username</h2>  <input id="username" placeholder="Username"/>  <button onclick="join()">Join</button></div><div class="container" id="chatApp" style="display:none;">  <div class="left">    <h3>Online Users</h3>    <div id="users"></div>  </div>  <div class="right">    <h3 id="chatWith" style="padding:10px;"></h3>    <div class="chat-box" id="messages"></div>    <div style="padding:10px;">      <input id="messageInput" placeholder="Type message..." style="width:80%"/>      <button onclick="sendMessage()">Send</button>    </div>  </div></div><script src="/socket.io/socket.io.js"></script><script>const socket = io();let username = "";let currentChat = "";function join() {  username = document.getElementById("username").value;  if (!username) return;  document.getElementById("login").style.display = "none";  document.getElementById("chatApp").style.display = "flex";  socket.emit("join", username);}socket.on("onlineUsers", (users) => {  const usersDiv = document.getElementById("users");  usersDiv.innerHTML = "";  users.filter(u => u !== username).forEach(user => {    const div = document.createElement("div");    div.className = "user";    div.innerHTML = "🟢 " + user;    div.onclick = () => selectUser(user);    usersDiv.appendChild(div);  });});function selectUser(user) {  currentChat = user;  document.getElementById("chatWith").innerText = "Chat with " + user;  document.getElementById("messages").innerHTML = "";}function sendMessage() {  const messageInput = document.getElementById("messageInput");  const message = messageInput.value;  if (!message || !currentChat) return;  const data = {    sender: username,    receiver: currentChat,    message,    time: new Date()  };  socket.emit("sendMessage", data);  messageInput.value = "";}socket.on("receiveMessage", (data) => {  if (data.sender !== currentChat && data.receiver !== currentChat) return;  const messagesDiv = document.getElementById("messages");  const div = document.createElement("div");  div.className = "message " + (data.sender === username ? "me" : "other");  div.innerHTML = "<div class='bubble'>" + data.message + "</div>";  messagesDiv.appendChild(div);  messagesDiv.scrollTop = messagesDiv.scrollHeight;});</script></body></html>
